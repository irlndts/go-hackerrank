package main

/*
func main() {
	var N, T int
	fmt.Scanf("%v %v", &N, &T)

	buf := N

	var result int
	for i := 0; i < (T - 1); i++ {
		var c int
		fmt.Scan(&c)

		buf = buf - c
		if buf < 5 {
			result += N - buf
			buf = N
		}
	}
	fmt.Println(result)
}
*/
