package main

import "fmt"

/*
func main() {
	var N, A int
	fmt.Scanf("%v %v", &A, &N)

	astronauts := make(map[int][]int)

	for i := 0; i < N; i++ {
		var self, friend int
		fmt.Scanf("%v %v", &self, &friend)
		astronauts[self] = append(astronauts[self], friend)
		astronauts[friend] = append(astronauts[friend], self)
	}

	fmt.Printf("%+v\n", astronauts)
	result := 0
	for _, v := range astronauts {
		result += A - len(v) - 1
	}
	fmt.Println(result / 2)
}
*/
